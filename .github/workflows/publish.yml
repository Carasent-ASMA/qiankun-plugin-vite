name: Build and Publish

on:
    push:
        branches:
            - master
    workflow_dispatch:

permissions:
    contents: write
    id-token: write

jobs:
    publish:
        uses: Carasent-ASMA/asma-workflows/.github/workflows/reusable-npm-publish.yml@master
        with:
            publish_npm: true
            package_name: "asma-qiankun-plugin-vite"
        secrets: inherit
